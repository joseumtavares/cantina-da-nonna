-- Criado por Jose Tavares, tomando como referência o primeiro passo de banco da aula.
-- Este arquivo existe para estudo manual do fluxo de criação do banco.
-- No projeto atual, o Spring também executa scripts automáticos em src/main/resources/db.

CREATE DATABASE IF NOT EXISTS `nona-db`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
